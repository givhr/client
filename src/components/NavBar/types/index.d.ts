import { VNav, HNav } from '../../../constants';
import { ThemeShadows } from '../../../theme/globalStyles/boxShadows';

export interface INavBar {
  height: string;
  width?: string;
  vert: boolean;
}
