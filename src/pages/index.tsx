import React, { Fragment } from 'react';
import Button from '../components/Button';

const IndexPage: React.FunctionComponent = () => (
  <Fragment>
    <Button height={'44px'} width={'150px'}>
      New Giveaway
    </Button>
  </Fragment>
);

export default IndexPage;
